# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.WordleController.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

# A Test to understand how it works
#GET     /play/:userInput           controllers.WordleController.play(userInput: String)
#POST    /play/:userInput          controllers.WordleController.play(userInput: String)
GET     /play/:userInput           controllers.WordleController.userInputforTUI(userInput: String)

# Wordle html
GET /play                           controllers.WordleController.game()